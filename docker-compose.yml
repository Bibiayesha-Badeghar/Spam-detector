services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - HOST=0.0.0.0
      - PORT=5000
      - FLASK_DEBUG=false
      - RETRAIN_API_KEY=change-me-in-production
    restart: unless-stopped
